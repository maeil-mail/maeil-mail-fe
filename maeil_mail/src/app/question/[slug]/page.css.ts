import { style } from '@vanilla-extract/css';

export const pageLayout = style({
  height: 'auto',
  minHeight: '100%',
  width: '85%',
  margin: '0 auto',
});

export const detailHeaderWrapper = style({
  // background: 'linear-gradient(180deg, rgba(255, 255, 255, 1) 0%, rgba(0, 168, 107, 0.1) 100%)',
});
